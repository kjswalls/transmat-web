{
  "name": "transmat",
  "private": true,
  "description": "Root task runner. Each component is standalone with its own node_modules.",
  "scripts": {
    "setup": "cd server && npm install && cd ../web && npm install && cd ../cli && npm install",
    "dev": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "test": "cd server && npm test",
    "cli": "cd cli && node src/index.js"
  }
}
